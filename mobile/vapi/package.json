{
  "name": "voicebridge-vapi-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge a Vapi phone call to a live Claude Code session (OpenAI-compatible custom LLM endpoint + channel)",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
